#include <stdio.h>
#include <math.h>
int main()
{
    int n;
    printf("Insert n: ");
    scanf("%d", &n);
    float pi = 0;
    for (int i = 0; i <= n; i++) {
        pi += (float)pow(-1, i) / (float)(2 * i + 1);
    }
    pi *= 4;
    printf("Pi value is: %f", pi);
}
